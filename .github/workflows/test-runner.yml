name: Test SelfHosted
on:
  push:
    branches: [main]
    paths: ['.github/workflows/test-runner.yml']
  workflow_dispatch:
jobs:
  ping:
    runs-on: windows-latest
    steps:
      - run: echo hello
        shell: pwsh
